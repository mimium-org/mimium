{
    "tasks": [
        {
            "type": "shell",
            "label": "cmake file",
            "command": "cmake",
            "args": [".."],
            "options": {
                "cwd": "${workspaceFolder}/build"
            }
        },{
            "type": "shell",
            "label": "make file",
            "command": "make",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}/build"
            }
        }
    ],
    "version": "2.0.0"
}