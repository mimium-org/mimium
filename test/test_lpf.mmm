
fn lpf(fb:float){    
    self  = 0
    fn perform(input:float){
        res = input*(1-fb) + self*fb
        self = res
        return res
    }
    return perform
}
//myfn = |x:float| x+2 
mylpf = lpf(1250)
fn dsp(time:float)->float{
    return time|> mylpf
}